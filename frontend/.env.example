# Absolute URL of the deployed backend (Render), e.g. https://weaponshield-backend.onrender.com
# Leave unset for local dev — Vite's dev-server proxy (vite.config.js) handles it instead.
VITE_API_URL=
